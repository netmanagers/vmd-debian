# Indent everything by spaces
* whitespace=trailing-space,tab-in-indent,tabwidth=4

# Makefile must be indented by tabs
debian/rules whitespace=-tab-in-indent
# Invalid spaces may occur in patches
debian/patches/*.patch whitespace=-trailing-space,-space-before-tab
